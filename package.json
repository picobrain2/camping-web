{
  "name": "camping-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "sync": "node scripts/sync-gocamping.mjs",
    "sync:all": "node scripts/sync-gocamping.mjs --all",
    "sync:photos": "node scripts/sync-photos.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "firebase": "^11.10.0"
  }
}
